<div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
            data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#">Kali BackOffice</a>
</div>

<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
        {% if not app.user %}
            <li class="active"><a href="{{ path("fos_user_security_login") }}">Connexion</a></li>
        {% else %}
            <li>
                <a href="{{ path("user.list") }}">Gestion des utilisateurs</a>
            </li>
            <li>
                <a href="{{ path("client.list") }}">Gestion des clients</a>
            </li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Gestion du catalogue<span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                    <li><a href="{{ path("product_list") }}">Produits</a></li>
                    <li class="divider"></li>
                    <li><a href="{{ path("category_list") }}">Catégories</a></li>
                    <li class="divider"></li>
                    <li><a href="{{ path("caracteristic_list") }}">Caractéristiques</a></li>
                    <li class="divider"></li>
                    <li><a href="{{ path("command.list") }}">Commandes</a></li>
                    <li class="divider"></li>
                    <li><a href="{{ path("promotion_list") }}">Coupons de réduction</a></li>
                    <li class="divider"></li>
                    <li><a href="{{ path("sender_list") }}">Transporteurs</a></li>
                </ul>
            </li>
            <li class="dropdown">
                <a href="{#{ path("parameters") }#}" class="dropdown-toggle" data-toggle="dropdown">Gestion des paramètres<span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                    <li><a href="{{ path("slider_plug") }}">Slider</a></li>
                    <li><a href="{{ path("parameters") }}">Paramètres</a></li>
                </ul>
            </li>
        {% endif %}
    </ul>
    <ul class="nav navbar-nav navbar-right">
        <li>
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <a href="{{ path('fos_user_security_logout') }}">Déconnexion ({{ app.user.username }})</a>
            {% endif %}
        </li>
    </ul>
</div><!-- /.navbar-collapse -->