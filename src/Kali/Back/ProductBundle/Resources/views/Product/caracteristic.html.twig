{% extends "::layout.html.twig" %}

{% block title %}Caracteristique de {{product.name}}{% endblock %}

{% block body %}
    {% include "KaliBackProductBundle:Product:navbar.html.twig" with {'active' : 'caract'} %}
    <h2>Caractéristique du produit : {{product.name}}</h2>

    {% for flashMessage in app.session.flashbag.get('notice') %}
        <div class="flash-notice">
            {{ flashMessage }}
        </div>
    {% endfor %}

    {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-group">
        {{ form_label(form.caracteristics) }}
        <div class="col-sm-10">
            {{ form_widget(form.caracteristics) }}
        </div>
    </div>

    <input type="submit" value="Sauvegarder" class="btn btn-success"/>
    <a href="#" class="btn btn-info" onclick="history.go(-1)">Retour</a>
    {{ form_end(form) }}

{% endblock %}
